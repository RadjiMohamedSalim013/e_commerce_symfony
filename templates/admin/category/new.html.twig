{% extends 'base.html.twig' %}

{% block title %}Nouvelle catégorie{% endblock %}

{% block body %}
<h1>Ajouter une catégorie</h1>

{{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit">Créer</button>
{{ form_end(form) }}

<a href="{{ path('admin_category_list') }}">Retour à la liste</a>
{% endblock %}
